package sakethh.tenmin.mail.ui.home.screens.search

import androidx.compose.runtime.mutableStateOf

class SearchContentVM {
    companion object {
        val isSearchEnabled = mutableStateOf(false)
    }
}